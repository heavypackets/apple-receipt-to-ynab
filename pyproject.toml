[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "apple-receipt-to-ynab"
version = "0.1.0"
description = "Parse Apple subscription receipt PDFs and create YNAB split transactions."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "httpx>=0.27.0",
  "pypdf>=4.2.0",
  "PyYAML>=6.0.1",
]

[project.scripts]
apple-receipt-to-ynab = "apple_receipt_to_ynab.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
